dist
node_modules
.env

# Rust build outputs
target

# Benchmark outputs (vitest bench --outputJson / criterion)
bench-results.json
bench-results

# Native binaries copied into platform packages — built in CI, never committed
packages/iso4-v8-*/bin/iso4-v8

# Local-only notes, never committed
internal

# OS / editor
.DS_Store
*.swp
.idea
